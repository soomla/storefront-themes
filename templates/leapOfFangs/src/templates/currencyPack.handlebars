<div class="visual">
    <img src="{{imgFilePath}}">
</div>
<div class="buy-container">
    <img src="{{images.buyImage}}" class="buy">
	<div class="price-container">
        <img src="{{currency.imgFilePath}}">
        <label class="price">{{price}}</label>
	</div>
</div>
<div class="description-container">
    <h1>{{name}}</h1>
    <p>{{description}}</p>
</div>
