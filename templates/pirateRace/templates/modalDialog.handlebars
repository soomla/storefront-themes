<div class="modal">
    <div class="dialog">
        <h1>{{text}}</h1>
        <div class="actions">
            <img src="{{noButton}}" class="cancel">
            <img src="{{yesButton}}" class="buy-more">
        </div>
    </div>
</div>
