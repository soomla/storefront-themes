<img src="{{imgFilePath}}" class="visual">
<div data-itemid="{{itemId}}" class="price font-face-2">
    <h1 style="{{nameStyle}}">{{name}}</h1>
    <h2 style="{{priceStyle}}">{{price}}$</h2>
</div>
<img src="{{itemSeparator}}" class="separator">
